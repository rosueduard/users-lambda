{
  "name": "users-lambda",
  "version": "1.0.0",
   "type": "module",
  "scripts": {
    "build:zip": "rm -rf dist && tsc && cd dist && cp ../package.json ./ && npm install --production && zip -r ../users-lambda.zip * && cd .."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.920.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "typescript": "^5.1.6"
  }
}
